# Copie para ~/.cerurb-cf.env (fora do repositório) e preencha. chmod 600.
# Token: Cloudflare → My Profile → API Tokens → Create → escopo
#        "Account · Cloudflare Pages · Edit" e nada além disso.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
